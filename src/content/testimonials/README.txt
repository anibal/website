# Testimonials live here as .md files with frontmatter (quote, name, role,
# company, lang, featured, serviceRef?) per src/content.config.ts.
# Handoff rule: real quotes only — the section renders nothing without ≥3.
# Kept in git via this file; delete once real testimonials land.
